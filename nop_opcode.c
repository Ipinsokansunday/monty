#include "monty.h"

/**
 * f_nop - does nothing
 * @stack_head: pointer to the stack head
 * @num_count: line number (unused)
 * Return: no return
 */
void f_nop(stack_t **stack_head, unsigned int num_count)
{
	(void)num_count;
	(void)stack_head;
}
