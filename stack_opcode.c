#include "monty.h"

/**
 * f_stack - prints the top
 * @stack_head: stack head
 * @num_count: line_number
 * Return: no return
 */
void f_stack(stack_t **stack_head, unsigned int num_count)
{
	(void)stack_head;
	(void)num_count;
	bus.lifi = 0;
}
