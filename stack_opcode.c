#include "monty.h"

/**
 * s_stack - prints the top
 * @stack_head: stack head
 * @num_count: line_number
 * Return: no return
 */
void s_stack(stack_t **stack_head, unsigned int num_count)
{
	(void)stack_head;
	(void)num_count;
	bus.lifi = 0;
}
